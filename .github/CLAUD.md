# CLAUDE.md

  This file gives Claude project-specific context. It's read
  automatically
  on every `@claude` mention.

  ## Project overview

  A working repository for the Anthropic API course. Code samples,
  exercises, and experiments live here.

  ## Conventions

  - (Add language/framework conventions as the codebase grows.)
  - (Add formatting/linting rules once you pick them.)

  ## Commands

  - (Add common commands here: how to run tests, lint, start the app,
  etc.)

  ## What to avoid

  - Don't commit API keys or `.env` files.
  - Don't push directly to `main` without a PR (if/when you adopt that
  rule).

  ---
